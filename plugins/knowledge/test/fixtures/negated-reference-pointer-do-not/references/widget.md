# negated-reference-pointer-do-not fixture — the (structurally valid) etalon

## Files

- `{shared-lib}/widget.ts`

**File:** `{shared-lib}/widget.ts`

```ts
export function widget() {
  return true
}
```
