# reference-pointer-variants fixture — SSR variant

Variant: projectType=ssr

## Files

- `{shared-lib}/thing.ts`

**File:** `{shared-lib}/thing.ts`

```ts
export function thing() {
  return "ssr"
}
```
