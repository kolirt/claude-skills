# reference-pointer-variants fixture — CSR variant

Variant: projectType=csr

## Files

- `{shared-lib}/thing.ts`

**File:** `{shared-lib}/thing.ts`

```ts
export function thing() {
  return "csr"
}
```
