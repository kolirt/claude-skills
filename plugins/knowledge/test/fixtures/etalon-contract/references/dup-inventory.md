# Duplicate inventory fixture

Violates the contract one way only: the same path is listed twice in `## Files`.

## Files

- `{shared-lib}/thing/a.ts`
- `{shared-lib}/thing/a.ts`

**File:** `{shared-lib}/thing/a.ts`

```ts
export const a = 1
```
