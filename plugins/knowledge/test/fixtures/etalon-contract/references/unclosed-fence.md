# Unclosed fence fixture

Violates the contract one way only: the fenced block after the marker is
opened and never closed with a closing ```` ``` ````.

## Files

- `{shared-lib}/thing/a.ts`

**File:** `{shared-lib}/thing/a.ts`

```ts
export const a = 1
